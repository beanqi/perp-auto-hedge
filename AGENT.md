# AGENT.md

本文件指导本项目的所有开发工作。

核心目标：**正确实现当前需求，同时持续降低代码复杂度和代码熵。**

```text
Simple > Clever
Concrete > Abstract
Explicit > Magical
Current Requirement > Future Flexibility
Small Diff > Architecture Rewrite
Delete > Add
```

## 1. 开发前
- 先阅读 `docs/业务设计.md` 和 `docs/架构设计.md`。
- 先理解现有数据流、状态机和模块边界，再修改代码。
- 当前结构能解决问题时，不修改架构。
- 不为“以后可能需要”提前设计。

## 2. 保持简单
- 优先更少的代码、类型、模块、状态、依赖、配置和间接调用。
- 能直接写清楚，就不要增加抽象层。
- 不要为了复用一两处代码创建 `Manager`、`Service`、`Factory`、`Framework` 或复杂 Trait 层级。
- 只有当重复已经真实存在，并且抽象能明显降低整体复杂度时才抽象。

## 3. 模块边界
```text
Exchange  -> 交易所协议与事实
Strategy  -> 交易价值计算
Risk      -> 风险许可
Execution -> 执行订单计划
Engine    -> 编排并维护状态
```
- 不要跨层偷放逻辑。
- 不要为了模块边界增加无意义的包装层。

## 4. 状态与数据流
- 状态必须有唯一明确的 owner。
- `PairState` 只能由 `Engine` 修改。
- 数据流必须可以从代码直接读出来。
- 优先显式 `match`，不要引入状态机框架或规则引擎。
- 异常状态显式处理，不依赖隐式副作用。

## 5. 实现规则
- 函数只做一件明确的事。
- 名字表达业务含义，不制造新术语。
- 优先普通 struct / enum / function。
- Trait 只在存在真实多实现需求时引入。
- 不引入当前需求不需要的通用能力。
- 不增加没有明确收益的依赖。
- 不隐藏重要控制流、网络调用、状态变化和资金操作。
- 错误带足够上下文，但不要构建复杂错误体系。

## 6. 修改原则
- 最小文件范围。
- 最小接口变化。
- 最少新概念。
- 最少新代码。
- 不顺手重构无关代码。
- 能通过删除、合并或简化实现时，优先这样做。

## 7. 完成前检查
提交前问自己：
1. 能不能删掉一些代码？
2. 能不能少一个类型、模块或状态？
3. 能不能直接写，而不是抽象？
4. 有没有为了未来加入当前不需要的东西？
5. 数据流和状态变化是否一眼可见？
6. 这个 diff 是否已经是解决当前问题的最小 diff？

如果还能更简单，继续简化后再提交。