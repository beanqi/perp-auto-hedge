[package]
name = "perp-auto-hedge"
version = "0.1.0"
edition = "2024"

[dependencies]
hmac = "0.12"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde_json = "1"
sha2 = "0.10"
tungstenite = { version = "0.28", features = ["rustls-tls-native-roots"] }
